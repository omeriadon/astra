name: Validate Release

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

jobs:
  validate-release:
    name: Validate Release
    runs-on: xcode-27

    steps:
      - name: Check out pull request
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select Xcode 27.2 beta
        shell: bash
        run: |
          sudo xcode-select -s /Applications/Xcode_27.2_beta.app/Contents/Developer
          xcodebuild -version

      - name: Install Metal Toolchain
        shell: bash
        run: xcodebuild -downloadComponent MetalToolchain

      - name: Parse and validate release metadata
        shell: bash
        run: |
          set -euo pipefail

          BRANCH="${{ github.head_ref }}"

          if [[ ! "$BRANCH" =~ ^release/([0-9]+\.[0-9]+(\.[0-9]+)?)\+([0-9]+)$ ]]; then
            echo "::error::Release branches must be named release/<version>+<build>, for example release/0.1.1+2"
            exit 1
          fi

          VERSION="${BASH_REMATCH[1]}"
          BUILD="${BASH_REMATCH[3]}"
          TAG="v$VERSION+$BUILD"

          echo "VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "BUILD=$BUILD" >> "$GITHUB_ENV"
          echo "TAG=$TAG" >> "$GITHUB_ENV"

          echo "Release branch: $BRANCH"
          echo "Version:        $VERSION"
          echo "Build:          $BUILD"
          echo "Tag:            $TAG"

          if git rev-parse --verify --quiet "refs/tags/$TAG" >/dev/null; then
            echo "::error::Release tag $TAG already exists"
            exit 1
          fi

      - name: Build release exactly as it will ship
        shell: bash
        run: |
          set -euo pipefail

          ARCHIVE="$RUNNER_TEMP/astra-validation.xcarchive"
          PACKAGES="$RUNNER_TEMP/SourcePackages"

          xcodebuild archive \
            -project astra.xcodeproj \
            -scheme astra \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -archivePath "$ARCHIVE" \
            -clonedSourcePackagesDirPath "$PACKAGES" \
            MARKETING_VERSION="$VERSION" \
            CURRENT_PROJECT_VERSION="$BUILD" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

          APP="$(find "$ARCHIVE/Products/Applications" -maxdepth 1 -name '*.app' -print -quit)"

          if [[ -z "$APP" || ! -d "$APP" ]]; then
            echo "::error::No .app was produced by the archive"
            exit 1
          fi

          ACTUAL_VERSION="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$APP/Contents/Info.plist")"
          ACTUAL_BUILD="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "$APP/Contents/Info.plist")"

          if [[ "$ACTUAL_VERSION" != "$VERSION" ]]; then
            echo "::error::Built app version is $ACTUAL_VERSION, expected $VERSION"
            exit 1
          fi

          if [[ "$ACTUAL_BUILD" != "$BUILD" ]]; then
            echo "::error::Built app build is $ACTUAL_BUILD, expected $BUILD"
            exit 1
          fi

          codesign \
            --force \
            --sign - \
            --entitlements astra/Special/astra.entitlements \
            "$APP"

          codesign --deep --strict --verify "$APP"

          echo "Validated Astra $ACTUAL_VERSION (build $ACTUAL_BUILD) with an ad-hoc code signature."
