# Project Memory

1. When implementing Sign in with Apple, use the Timetable server as inspiration.
2. Work in small turns. Consult the user before implementing each slice; never start the next slice autonomously.
3. After user approval for an implementation slice, delegate it to one Luna medium subagent.
4. Do not launch or operate the app through computer-use tools. Limit verification to source inspection and Xcode MCP diagnostics or builds when explicitly needed.
5. Never use Graphify in this project. Inspect the small codebase directly.
